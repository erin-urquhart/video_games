<div class="header">
  <h1 class="title is-3">Genres</h1>
</div>

<div class="results">
  <ul class="columns is-multiline mt-5">
    <% @genres.each do |genre|%>
      <li class="column box is-one-quarter has-background-primary-light m-5"><%= link_to genre.name, genre %></li>
    <% end %>
  </ul>
</div>