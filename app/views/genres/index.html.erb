<div class="header">
  <h1 class="title is-3">Genres</h1>
</div>

<div class="results">
  <div class="columns is-multiline mt-5">
    <% @genres.each do |genre|%>
      <div class="column box is-one-quarter has-background-primary-light m-5"><%= link_to genre.name, genre %></div>
    <% end %>
  </div>
</div>